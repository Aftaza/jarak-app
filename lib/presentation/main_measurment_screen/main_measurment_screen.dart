import 'package:flutter/material.dart';

class MainMeasurementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Main Measurement'),
      ),
      body: Center(
        child: Text('Main Measurement Screen'),
      ),
    );
  }
}
