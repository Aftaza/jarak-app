import 'package:flutter/material.dart';

class MeasurementResultScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Measurement Result'),
      ),
      body: Center(
        child: Text('Measurement Result Screen'),
      ),
    );
  }
}
